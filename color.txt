#555 — Used for text color in the body (color: #555). '
#003380 — Used for text color (e.g., .company-name, .forgot-password:hover, etc.) and the background color of buttons like .login-btn and hover effect.
#f0f0f0 — Used for background colors (e.g., .login-background, .login-container, .logo-container, etc.),
text color in .forgot-password,
and the background color of the login form inputs. 
#0076d7
#002966 — Used for the border color in input fields (border: 1px solid 
#e6ebf2 — Used as the background color on input focus (background-color: #e6ebf2).
#868e96 — Used for text color in .forgot-password,
.show-pwd svg,
and for hover states. 
#343a40 — Used for the text color in .display-text and .checkboxLabel. 
#ff6b6b — Used for error message text color (.error-message).